<%- include('../partials/header2.ejs'); %>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css">
<style>
    body {
        background: linear-gradient(to bottom, #e3f2fd, #ffffff);
        font-family: 'Arial', sans-serif;
        color: #343a40;
        padding-bottom: 50px;
    }
    .report-card {
        margin: 50px auto;
        border-radius: 15px;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        transition: transform 0.3s;
    }
    .report-card:hover {
        transform: translateY(-10px);
    }
    .report-header {
        padding: 40px;
        background: linear-gradient(90deg, #007bff, #0056b3);
        color: white;
        border-top-left-radius: 15px;
        border-top-right-radius: 15px;
        text-align: center;
    }
    .report-header h1 {
        font-size: 2.8rem;
        margin-bottom: 10px;
        font-weight: bold;
    }
    .report-header p {
        font-size: 1.5rem;
    }
    .member-detail {
        padding: 25px;
        margin-bottom: 15px;
        border-radius: 10px;
        background-color: #f8f9fa;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        transition: background-color 0.3s, transform 0.3s;
    }
    .member-detail:hover {
        background-color: #e9ecef;
        transform: scale(1.02);
    }
    .total {
        font-weight: bold;
        font-size: 1.5rem;
        color: #007bff;
        margin-top: 20px;
    }
    h2 {
        margin-top: 30px;
        border-bottom: 3px solid #007bff;
        padding-bottom: 10px;
        position: relative;
        font-weight: bold;
    }
    h2::before {
        content: '';
        position: absolute;
        left: 0;
        bottom: -5px;
        height: 5px;
        width: 60px;
        background-color: #007bff;
    }
    .icon {
        font-size: 1.5rem;
        margin-right: 10px;
    }
    .share-buttons {
        margin-top: 20px;
        display: flex;
        justify-content: center;
    }
    .share-button {
        margin: 0 10px;
        padding: 10px 15px;
        font-size: 1rem;
        background-color: #28a745;
        color: white;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }
    .share-button:hover {
        background-color: #218838;
    }
    .footer {
        text-align: center;
        padding: 20px;
        font-size: 0.9rem;
        color: #6c757d;
        position: relative;
        bottom: 0;
        width: 100%;
    }
</style>
</head>
<body>
    <div style="margin-top: 200px;" class="container">
        <div class="card report-card">
            <div class="report-header">
                <h1><i class="bi bi-church"></i> Relatório de Dízimos <i class="bi bi-cash-stack"></i></h1>
                <p>Período Selecionado: <strong><%= startDate.toLocaleDateString() %> a <%= endDate.toLocaleDateString() %></strong></p>
            </div>
    
            <div class="card-body">
                <h2><i class="bi bi-file-earmark-richtext icon"></i> Resumo do Relatório:</h2>
                <p>
                    Durante o período selecionado, a igreja recebeu uma quantidade significativa de dízimos,
                    demonstrando o comprometimento e a generosidade dos membros.
                </p>
                <div class="total">
                    <i class="bi bi-graph-up-arrow icon"></i> Total de Dízimos Recebidos: Kz <%= totalGeral.toFixed(2) %>
                </div>
                
                <h2><i class="bi bi-file-earmark-x icon"></i> Despesas Incluídas:</h2>
                <div class="total">
                    <i class="bi bi-graph-down-arrow icon"></i> Total de Despesas: Kz <%= totalDespesas.toFixed(2) %>
                </div>
                
                <div class="total">
                    <i class="bi bi-cash-stack icon"></i> Total Restante: Kz <%= totalRestante.toFixed(2) %>
                </div>
    
                <h2><i class="bi bi-list-task icon"></i> Detalhes dos Dízimos:</h2>
                <div class="row">
                    <% for (const membroId in totalPorMembro) { %>
                        <div class="col-md-4">
                            <div class="member-detail">
                                <h3><i class="bi bi-person-circle icon"></i> <%= nomesPorMembro[membroId] %>:</h3>
                                <p><i class="bi bi-cash-coin icon"></i> Total de Dízimos: Kz <%= totalPorMembro[membroId].total.toFixed(2) %></p>
                                <p><i class="bi bi-file-earmark-text icon"></i> Número de Dízimos: <%= totalPorMembro[membroId].quantidade %></p>
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>
            <div class="total">
                <i class="bi bi-people-fill icon"></i> Total de Membros que Contribuíram: <%= Object.keys(totalPorMembro).length %>
            </div>
        </div>
       
    </div>
    
            

            <div class="share-buttons">
                <button class="share-button"><i class="bi bi-whatsapp"></i> Compartilhar no WhatsApp</button>
                <button class="share-button"><i class="bi bi-envelope-fill"></i> Enviar por Email</button>
            </div>
        </div>
    </div>
</div>

<div class="footer">
    <p>&copy; 2024 Sua Igreja. Todos os direitos reservados.</p>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
